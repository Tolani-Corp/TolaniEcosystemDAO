{
  "name": "skillsbuild-webhook",
  "version": "1.0.0",
  "description": "IBM SkillsBuild webhook server for TUT training rewards",
  "main": "skillsbuild-webhook.js",
  "scripts": {
    "start": "node skillsbuild-webhook.js",
    "dev": "nodemon skillsbuild-webhook.js",
    "test": "node test-webhook.js"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "ethers": "^6.16.0",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
